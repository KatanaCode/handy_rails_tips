
function tipUpdated(){
	var val = $('tip_body').value;
	$('preview_tip').innerHTML = '<%= tip_body("[text][/text][ruby]def some_ruby;end[/ruby][text][/text]") %>';
	
}

document.observe('dom:loaded', function() {
	tipUpdated();
	$('tip_body').observe("keyup", tipUpdated);
});
